/**
 * Legacy import path — the dialog now lives in the shared UI library and is
 * built on the accessible Modal shell (Escape, focus trap, ARIA).
 */
export { ConfirmDialog } from '@/components/ui/ConfirmDialog';
export type { ConfirmDialogProps } from '@/components/ui/ConfirmDialog';
