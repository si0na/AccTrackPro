# Backend API URL — only needed in production builds.
# In development, Vite proxies /api to http://localhost:3000 automatically.
VITE_API_URL=

# App title (optional, used in document.title)
VITE_APP_TITLE=AccTrack Pro
